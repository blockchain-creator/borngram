
# type FullPost{
  # post: Post!
  # files: [File!]!
  # comment: [Comment!]!
  # likeCount: Int!
  # user: User!
# }

type Query{
    seeFullPost(id: String!): Post!
    # seeFullPost(id: String!): FullPost!
}